# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Env files at any depth; per-app .env.example files are allow-listed
# (do NOT anchor these with a leading slash — each app's .env.example relies on the depth-wide re-include)
.env
.env.*
!.env.example
